{
  "name": "bhusetu-root",
  "version": "1.0.0",
  "description": "Root workspace setup for BhuSetu Ownership-State Verification Engine",
  "scripts": {
    "setup": "npm run install-backend && npm run install-frontend && npm install",
    "install-backend": "npm install --prefix backend",
    "install-frontend": "npm install --prefix frontend",
    "start": "concurrently --kill-others \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "test": "npm run test --prefix backend"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
